package model

/**
 * Notification class represents a notification model with properties such as title, content, and timestamp.
 * It is a simple data class with read-only properties.
 */
class Notification(
    val title: String,
    val content: String,
    val timestamp: String
)
